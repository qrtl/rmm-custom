Before downloading the images, run the scheduler 'Update Product Image
Name' to ensure that existing images have the correct file extensions.
This is necessary because product images uploaded manually use the
product name by default and often lack a proper file extension. This
step is only required for images that existed before installing this
module.
